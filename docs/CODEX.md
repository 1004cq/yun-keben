# 给 Codex 用本仓库

打开仓库后 skill 应自动可用，不必再装。
Codex 会扫 `.agents/skills/`。

若列表里没有：
```bash
codex plugin marketplace add .
codex plugin add yun-keben@yun-keben-marketplace
```
不要自动装 React Bits / Aceternity。
